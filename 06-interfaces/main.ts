//how to build interfaces
var drawPoint = function (point) {
    //..
};
drawPoint({
    x: 1,
    y: 2
});
/*
//inline annotation -------------- too complex

let drawPoint = (point: { x: number, y: number}) => {
    //....
}

drawPoint({
    x: 1,
    y: 2
})

*/ 
